mixin article(data = {})
  - const { defaults, mergeConfig } = require("@defaults/article/_defaults");
  - const modefied = mergeConfig(data, defaults);

  a.article(href=modefied.href target=modefied.target)
    h3= modefied.title
    span Перейти