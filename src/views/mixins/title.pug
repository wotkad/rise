mixin title(data = {})
  - const { defaults, mergeConfig } = require("/assets/js/_defaults/title/_defaults");
  - const modefied = mergeConfig(data, defaults);

  section.title
    .wrapper
      h1 !{ modefied.title }